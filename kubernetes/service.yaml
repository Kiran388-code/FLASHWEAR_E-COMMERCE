apiVersion: v1
kind: Service
metadata:
  name: flashwear-backend-service
  namespace: default
  labels:
    app: flashwear-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: flashwear-backend
